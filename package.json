{
  "name": "expect-to-assert",
  "version": "1.0.0",
  "description": "jscodeshift code mod to convert jest-style expects to assert",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "codemod",
    "jscodeshift",
    "expect",
    "assert"
  ],
  "author": "Scott Trinh <scott@scotttrinh.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/jscodeshift": "^0.11.6",
    "@types/node": "^20.2.0",
    "jscodeshift": "^0.15.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "zod": "^3.21.4"
  }
}
